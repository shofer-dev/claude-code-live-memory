---
description: Empty the Live Memory — wipe its accumulated knowledge for this workspace (or `all`) so the next question starts from a blank slate. Human-facing.
argument-hint: "[all]"
allowed-tools: ["Bash"]
---

Run the Live Memory empty command and show the output to the user verbatim.

!`python3 "${CLAUDE_PLUGIN_ROOT}/commands/empty.py" $ARGUMENTS`
