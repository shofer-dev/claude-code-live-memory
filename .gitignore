__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
*.egg-info/
.venv/
build/
dist/
# runtime state (lives in the data dir, but guard against in-tree runs)
config.json
oauth_state.json
*.tmp

# python build artifacts
server/dist/
server/build/
server/*.egg-info/
